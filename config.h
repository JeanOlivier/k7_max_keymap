#define TAPPING_TERM 150
/* Delay for tap/hold for tap dance commands */
//#define FORCE_NKRO
#define LEADER_PER_KEY_TIMING   // Each key reset the leader delay
#define LEADER_TIMEOUT 200      // 200 ms delay between key presses
#define LEADER_NO_TIMEOUT       // Won't use the delay for the first key
